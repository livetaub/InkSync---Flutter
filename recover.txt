[]
[]
[]
[{"name": "view_file", "args": {"AbsolutePath": "c:\\My Projects\\InkSync\\lib\\main.dart", "EndLine": 250, "StartLine": 1, "toolAction": "View file", "toolSummary": "View main.dart"}}]
[{"name": "view_file", "args": {"AbsolutePath": "c:\\My Projects\\InkSync\\lib\\screens\\calendar\\calendar_screen.dart", "EndLine": 400, "StartLine": 1, "toolAction": "View file", "toolSummary": "View calendar_screen.dart"}}]
[]
[{"name": "view_file", "args": {"AbsolutePath": "c:\\My Projects\\InkSync\\lib\\screens\\calendar\\calendar_screen.dart", "EndLine": 723, "StartLine": 401, "toolAction": "View file", "toolSummary": "View rest of calendar_screen.dart"}}]
[]
[]
[]
[{"name": "send_message", "args": {"Message": "# InkSync Codebase Research Report: Calendar, Notes, Checklists, Settings & Navigation\n\n## 1. Calendar Functionality\n\n### Current Architecture & State\n- **Status:** Fully coded and functional at the component level, but **disabled from the main navigation and editor action menus** (commented out in `main_navigation.dart` and `note_edit_screen.dart` when the app transitioned to dedicated Notes vs Checklists vs Brain Dump tabs).\n- **Core Concept:** There is **no separate `calendar_events` table** in the database. Instead, calendar events are **derived directly from notes with reminders** (`note.reminderAt != null`).\n\n### Model (`lib/services/calendar_service.dart` -> `CalendarEvent`)\n- **Fields:** `id`, `title`, `description`, `date`, `isAllDay` (default true), `startTime`, `endTime`, `isReminder` (default false), `color`, `noteId`, `createdBy`, `createdDate`, `updatedDate`.\n- **Factory:** `CalendarEvent.fromNote(Note note)` extracts `date` from `note.reminderAt`, `title` from `note.title`, `description` from `note.content`, and color from `note.color`.\n\n### Service (`lib/services/calendar_service.dart` -> `CalendarService`)\n- Wraps `NotesService` and queries active notes:\n  - `getEvents()`: Fetches all active notes where `reminderAt != null` and maps them to sorted `CalendarEvent`s.\n  - `getEventsForDate(DateTime date)`: Filters events falling within the 24-hour day window.\n  - `getEventsForMonth(int year, int month)`: Filters events falling within the given month.\n  - `createEvent(CalendarEvent event)`: If `noteId` exists, updates `reminderAt` on that note; otherwise creates a brand-new `Note` with `reminderAt: event.date`.\n  - `updateEvent(String eventId, Map updates)`: Updates the note's `reminderAt`.\n  - `deleteEvent(String eventId)`: Sets the note's `reminderAt` to `null`.\n\n### Screen & UI (`lib/screens/calendar/calendar_screen.dart` -> `CalendarScreen`, `EventFormSheet`)\n- **Calendar Screen Layout:**\n  - **Header:** Previous/Next month chevrons and formatted month-year title.\n  - **Weekday Labels:** Sun through Sat header.\n  - **Grid View (7 columns):** Days of current month with offset padding for start weekday. Highlights today, selected day, and shows a dot indicator for days with events.\n  - **Bottom Events List:** Card list showing events for the selected day with left color bar accent, notification bell icon, title, description preview, and time interval / \"All day\" badge.\n  - **FAB (+):** Opens `EventFormSheet` bottom sheet modal.\n- **`EventFormSheet`:**\n  - Form allowing event title, description, date picker dialog, All-day toggle switch, Start/End time dropdowns (30-minute intervals), Reminder notification toggle switch, Color picker palette, and Save/Delete buttons.\n\n---\n\n## 2. Notes Feature\n\n### Model (`lib/services/notes_service.dart` -> `Note`)\n- **Fields:**\n  - `id`: UUID (String)\n  - `title`: String\n  - `content`: String\n  - `type`: String (`'text'` or `'checklist'`)\n  - `checklistItems`: `List<ChecklistItem>`\n  - `color`: String (`'yellow'`, `'orange'`, `'red'`, `'pink'`, `'purple'`, `'blue'`, `'teal'`, `'green'`)\n  - `isPinned`, `isPinnedToNotifications`: Booleans\n  - `isLocked`, `lockPassword`: Booleans / password strings\n  - `trashedAt`: ISO8601 String (for soft deletes)\n  - `reminderAt`: `DateTime?`\n  - `collaborators`: `List<Collaborator>` (with `email`, `accepted`, `canEdit`)\n  - `createdBy`, `createdDate`, `updatedDate`\n  - `tags`: `List<String>`\n  - `titleSetManually`: Boolean (differentiates user-typed titles from auto-derived titles)\n- **Serialization:** `toMap()`, `fromSupabase()`, `toSupabase()`, `copyWith()`.\n\n### Services Architecture\n- **Dual Architecture (Offline-First Mobile vs Direct Web):**\n  - **Web:** Calls Supabase directly using `SupabaseClient` with PostgreSQL RLS policies.\n  - **Mobile:** Uses `LocalDatabaseService` (SQLite database `inksync_local.db`) as the single source of truth. Writes locally first with sync tracking (`sync_status`: `synced=0`, `pendingInsert=1`, `pendingUpdate=2`, `pendingDelete=3`).\n  - **`SyncEngine` (`lib/services/sync_engine.dart`):** Runs two-way delta sync between SQLite and Supabase upon app launch, data changes, or manual trigger.\n- **`NotesService` (`lib/services/notes_service.dart`):**\n  - CRUD: `getNotes()`, `getActiveNotes()` (owned + accepted shared notes), `getTrashedNotes()`, `getNote(id)`, `createNote(note)`, `updateNote(id, map)`, `deleteNote(id)`.\n  - Trash Management: `trashNote(id)`, `restoreNote(id)`, `emptyTrash()`.\n  - Collaborations: `getPendingInvites()`, `acceptInvite(id)`, `rejectInvite(id)`, `leaveNote(id)`, `sendCollaborationInvite(noteId, email, canEdit)`, `updateCollaboratorPermission()`, `revokeInvite()`.\n  - Snapshots (Public read-only freeze copies): `createNoteSnapshot()`, `getSnapshot(token)`, `importFromSnapshot()`.\n- **`TagService` (`lib/services/tag_service.dart`):** Manages user tags by type (`'text'` vs `'checklist'`), reordering, renaming, and deleting.\n\n### Screens & Widgets\n- **`HomeScreen` (`lib/screens/home/home_screen.dart`):**\n  - Displays list/grid of notes filtered by `noteTypeFilter` (`'text'`, `'checklist'`, or `null`).\n  - Horizontal tag chip filter bar (\"All\" + user tags).\n  - Global Search bar (searches across title, content, checklist items, tags, and trashed notes).\n  - View modes: List (condensed), Details (expanded preview), and Grid.\n  - Sorting: Modified time, Created time, Alphabetical, Color, Reminder time.\n  - Context menu on long-press (Pin/Unpin, Move to Trash / Leave Note, Open).\n  - Top `InviteBanner` for real-time pending collaboration invites.\n- **`NoteEditScreen` (`lib/screens/note_edit/note_edit_screen.dart`):**\n  - Auto-derives title from the first non-empty line unless `titleSetManually` is set.\n  - Text editor with custom in-note search with match stepping and highlight scrolling.\n  - AI Assistant powered by Gemini (`GeminiService`): Proofread, Rephrase, Expand, Summarize, and Make Actionable.\n  - Password lock protection with secure email reset tokens via Supabase RPC (`request_note_unlock`).\n  - Read-only Shareable Snapshots with public token URL generation (`/snapshot/:token`).\n  - Real-time collaboration management modal with invite status indicators and Editor/Viewer permissions.\n  - Tag assignment sheet, Color picker sheet.\n- **`NoteCard` (`lib/widgets/note_card.dart`):**\n  - Dynamic display title and content preview (shows first line / checklist item summary).\n  - Pin, lock, and collaboration badge icons.\n  - Tactile styling with left border color accent, hover/press states, and selection shake animation.\n\n---\n\n## 3. Checklists Feature\n\n### Model & Data Structure\n- `ChecklistItem` (`lib/services/notes_service.dart`):\n  - `id`: String (timestamp or UUID)\n  - `text`: String\n  - `checked`: bool\n- In Supabase, stored as a `JSONB` array column `checklist_items`. In SQLite, serialized as a JSON string.\n- Notes with checklists have `type: 'checklist'`.\n\n### Navigation & Segregation\n- Checklists are **first-class citizens** in InkSync:\n  - Tab 1 in `MainNavigation` is dedicated to Notes (`HomeScreen(noteTypeFilter: 'text')`).\n  - Tab 2 in `MainNavigation` is dedicated to Checklists (`HomeScreen(noteTypeFilter: 'checklist')`).\n  - Tags are scoped by type (`type = 'text'` vs `type = 'checklist'`), allowing users to have distinct tags for checklists (e.g. \"To Do\", \"Groceries\") versus notes (e.g. \"Personal\", \"Work\").\n\n### UI & Interaction (`_buildChecklistEditor` in `NoteEditScreen`)\n- Built using `ReorderableListView.builder`:\n  - **Drag Handle:** Compact drag handle to reorder list items with haptic feedback.\n  - **Checkboxes:** Tapping a checkbox strikes through the item text with `TextDecoration.lineThrough` and dims the color.\n  - **Trash Can Icon:** Completed/checked items reveal a red delete button on the right edge.\n  - **Enter Key (`onSubmitted`):** Automatically adds a new checklist item below the current one and focuses the new text field immediately.\n  - **\"Add item\" Button:** Sticky button at the bottom to insert a new entry.\n  - **Conversion:** Note type can be converted back and forth:\n    - Text -> Checklist: splits content by newline into checklist items.\n    - Checklist -> Text: joins checklist items into newline-separated content.\n\n---\n\n## 4. Settings System\n\n### Architecture\n- **Model:** `UserSettings` (`lib/services/settings_service.dart`):\n  - `viewMode`: `'list'`, `'details'`, or `'grid'`\n  - `sortBy`: `'modified'`, `'created'`, `'alphabetical'`, `'color'`, `'reminder'`\n  - `darkMode`: bool\n  - `notificationsEnabled`: bool\n  - `isPremium`: bool\n- **Provider:** `SettingsProvider` (`lib/providers/settings_provider.dart`):\n  - In-memory `ChangeNotifier` providing instant UI reactivity without page reload.\n  - `saveSetting(authService, key, value)` updates provider state first, then calls `SettingsService` in the background.\n- **Service:** `SettingsService` (`lib/services/settings_service.dart`):\n  - Web uses Supabase table `user_settings`.\n  - Mobile uses SQLite table `user_settings` and synchronizes via `SyncEngine`.\n  - Validates key updates against a column allowlist.\n\n### Settings UI (`lib/screens/settings/settings_screen.dart` -> `SettingsScreen`)\n- Supports embedded dialog mode (`isDialog: true` for web desktop) and full-screen view for mobile.\n- **Sections & Subviews:**\n  1. **Display:** View mode selector (List, Details, Grid), Sort By options (Modified, Created, Alphabetically, By color, Reminder time).\n  2. **Tags:**\n     - \"Manage Note Tags\" (`type: 'text'`)\n     - \"Manage Checklist Tags\" (`type: 'checklist'`)\n     - Features `ReorderableListView` to drag-to-reorder tags, edit tag name dialog, delete tag dialog, and add tag dialog.\n  3. **Appearance:** Dark mode toggle switch (updates `ThemeProvider`).\n  4. **Notifications:** Push notification toggle switch.\n\n---\n\n## 5. Main Navigation Structure\n\n### Routing & Initialization (`lib/main.dart`)\n- **Initial Route:** Web starts at `/` (`LoginScreen`); Mobile starts at `/app` (`AuthWrapper`).\n- **Route Definitions:**\n  - `/` & `/login`: `LoginScreen`\n  - `/app` & `/home`: `AuthWrapper` -> `MainNavigation`\n  - `/shared/:token`: `SharedNoteScreen`\n  - `/snapshot/:token`: `SnapshotViewerScreen` (public read-only viewer)\n  - `/note-unlock`: `NoteUnlockVerificationScreen` (password reset)\n  - `/checkout` & `/checkout-success`: Subscription paywall checkout screens\n\n### Responsive Navigation Modes (`lib/screens/navigation/main_navigation.dart`)\n- **Desktop / Wide Screen (> 900px): 3-Pane Layout**\n  1. **Left Pane:** `WebSidebar` (persistent glassmorphism sidebar with + New note/checklist buttons, Search button, Notes tab, Checklists tab, Brain Dump tab, Menu expansion with Settings, Subscription, Pending Invites, Trash, Help, Sign Out).\n  2. **Middle Pane:** Resizable Note List (`_noteListWidth` between 280px - 600px with draggable divider), displaying `HomeScreen` or `TrashScreen` inside an `IndexedStack`.\n  3. **Right Pane:** Embedded `NoteEditScreen` for the currently selected note in `SelectionProvider` (or empty state placeholder).\n\n- **Mobile (<= 900px): Bottom Navigation Bar**\n  - **Tab 0:** Notes (`HomeScreen(noteTypeFilter: 'text')`)\n  - **Tab 1:** Checklists (`HomeScreen(noteTypeFilter: 'checklist')`)\n  - **Tab 2:** Brain Dump (`BrainDumpScreen` for quick audio/image/text capture)\n  - **Tab 3:** Global Search (activates inline search bar in current screen)\n  - **Tab 4:** Menu (opens `MainMenuSheet` bottom modal)\n  - **Floating Action Button (+):** Opens modal to pick between creating a **Note** or **Checklist**.\n\n---\n\n## 6. Supabase Database Migrations\n\n- **`cleanup_and_upsert.sql` & `collaboration_rls.sql`:**\n  - Collaboration invites table (`collaboration_invites`) with unique constraint on `(note_id, to_email)`.\n  - Database trigger `send_collaboration_invite_email` calling Resend HTTP email API when invites are created.\n  - RLS policies allowing accepted collaborators with `can_edit=true` to view and update notes.\n- **`create_note_snapshots.sql`:**\n  - Table `note_snapshots` storing frozen note copies with unique UUID public tokens and `content_hash` for deduplication.\n  - Public read RLS policy (`USING (true)`).\n- **`20260724200000_brain_dump.sql`:**\n  - Table `brain_dumps` for media types (`text`, `voice`, `image`, `pdf`).\n- **`note_reset_tokens.sql`:**\n  - Table `note_reset_tokens` and security-definer function `request_note_unlock` to send secure password reset emails for locked notes.\n- **`auto_empty_trash.sql`:**\n  - `pg_cron` schedule running daily at midnight (`0 0 * * *`) deleting trashed notes where `trashed_at < NOW() - INTERVAL '30 days'`.\n", "Recipient": "8864b66c-d1fd-4ce5-88cf-bec37ea556b5", "toolAction": "Send research report", "toolSummary": "Sending complete research report to parent agent"}}]
